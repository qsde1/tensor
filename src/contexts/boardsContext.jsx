import { createContext } from "react";

export const myBoardsContext = createContext(null);
export const setMyBoardsContext = createContext(null);

export const itemsMenuSideBarContext= createContext(null);
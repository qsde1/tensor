import { createContext } from "react";

//stores contexts
export const UserStoreContext = createContext(null);
export const TasksListsStoreContext = createContext(null);
export const ProjectStoreContext = createContext(null);


//local contexts